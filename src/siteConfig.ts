export const firstName = "Jayesh";
export const lastName = "Chaudhari";
export const description = "A passionate and creative developer building modern web experiences.";

const siteConfig = {
  name: `${firstName} ${lastName}`,
  // Add other global config as needed
};

export default siteConfig; 